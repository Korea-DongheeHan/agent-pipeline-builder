# 태스크: process-heavy (대량 처리 경로)

## 절차
선행 노드 `prepare`의 산출물(하단 컨텍스트)을 입력으로 대량 처리를 수행하라.
(이 템플릿을 실제 태스크에 맞게 수정)

## 규칙
- 멱등성: 재실행 시 중복 처리가 발생하지 않아야 한다.
- 부분 실패가 가능하면 처리/실패 건수를 산출물에 명시하라.

## 판정
전체 처리 성공 시 SUCCEEDED, 처리 불가·데이터 훼손 위험 시 FAILED.
